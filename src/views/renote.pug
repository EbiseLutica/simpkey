extends _base

block content
    if canRenote
        h2 このノートをリノートしますか？
        +sub-note(note)
        +post-form('/action/create-note', "コメント(省略可能)", "リノート")
            input(type="hidden", name="renoteId", value=note.id)
    else
        h2 このノートはリノートできません
        +sub-note(note)